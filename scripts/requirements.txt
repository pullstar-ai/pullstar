# PullStar dependencies are declared in pyproject.toml. This file is retained
# for backwards compatibility with `pip install -r scripts/requirements.txt`.
#
# Run from the repository root:
#
#   pip install .            # analysis engine only (no UI)
#   pip install ".[ui]"      # engine + Gradio demo application (app.py)
#   pip install -e ".[dev]"  # editable install + test tooling
#
-e .[ui]
